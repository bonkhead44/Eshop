import { Typography } from "@mui/material";

const ContactPage = () => {
    return(
        <Typography variant="h2">
            contact page
        </Typography>
    );
}

export default ContactPage;